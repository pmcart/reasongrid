{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm ci && npm run shared:build && npm run api:build && npx prisma generate --schema=prisma/schema.prisma"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy --schema=prisma/schema.prisma && npx tsx prisma/seed.ts && node apps/api/dist/index.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
